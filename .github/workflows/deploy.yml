name: React-API
on:
  push:
    branches:
      - main
jobs:
  deploy:
    name: React-API
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: ["12.x"]
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
      width:
        node-version: ${{matrix.node-version}}
      - name: install packages
      run: npm install
      - name: registry github user
      run: /
        git config --global user.email "dricardosc15@gmail.com"
        git config --global user.name "rdani2005"
      - name: deploy the app
      run: npm run deploy
      env:
        GH_TOKEN: ${{ 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 }}